
- Put skill in `conf/claude-local-marketplace/skills/` folder.
- Update `conf/claude-local-marketplace/skills/`, do not modify `~/.claude` directly.
- If nix fetch nix-priv have access issue, make sure run `ssh-add ~/.ssh/deploy`
