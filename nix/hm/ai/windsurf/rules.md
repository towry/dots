# Cascade Agent Rules

## Tools

- `find_code_context` AKA fast context in cascade
- **Critical**: When running shell commands, always append ` && exit 0` to prevent shell from hanging, this is important !!!!

@CONTENT@
